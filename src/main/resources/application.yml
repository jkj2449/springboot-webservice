spring:
  http:
    encoding:
      charset: UTF-8
      enabled: true

  profiles:
    include: oauth

  jpa:
    show_sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

  h2:
    console:
      enabled: true